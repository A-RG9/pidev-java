# Health module DB — used by integ (JournalSante) module
# Points to wellora_health (created by schema.sql)
# Set to same URL as db.properties if you merged both schemas into one DB
db.url=jdbc:mysql://localhost:3306/wellora_health
db.username=root
db.password=
db.driver=com.mysql.cj.jdbc.Driver
